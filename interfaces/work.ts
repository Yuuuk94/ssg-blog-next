type WorkType = {
  slug: string;
  title: string;
  excerpt: string;
  coverImage: string;
  date: string;
  project: string;
  URL: {
    demo: string;
    git: string;
  };
  Image: {
    url: string;
  };
  content: string;
};

export default WorkType;
