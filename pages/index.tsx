import type { NextPage } from "next";
import Layout from "../components/layout";
import List from "../components/list";

const Home: NextPage = () => {
  return (
    <Layout>
      <List />
    </Layout>
  );
};

export default Home;
